<div class="card my-3 ">
  <h5 class="card-header">
    <% if @message.outbox.user.is_doctor? %>Dr.<% end %>
    <%= @message.outbox.user.full_name %>
  </h5>
  <div class="card-body">
    <p class="card-text"><%= @message.body %></p>
    <%#
      we need a reference of original message (because no DB connection, only virtual attribute)
    %>
    <%= link_to "Reply", new_message_path(original_message_id: @message&.id), class: "btn btn-primary", style: "float: left; margin-right: 10px;" %>
    <div style="float: left; margin-right 10px;"><%= button_to "I've lost my prescription", messages_path, params: { message: { body: "I've lost my script, please issue a new one at a charge of €10", payment_ref: "abc123"}}, class: "btn btn-primary" %></div>
  </div>
</div>